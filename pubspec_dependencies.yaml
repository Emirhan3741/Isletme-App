# Bu dosya mevcut pubspec.yaml dosyanıza eklenecek bağımlılıkları içerir
# Belge yükleme sistemi için gerekli paketler

dependencies:
  # Firebase paketleri
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  firebase_auth: ^4.15.3
  firebase_storage: ^11.6.0
  
  # State Management
  provider: ^6.1.1
  
  # Dosya işlemleri
  file_picker: ^6.1.1
  
  # UI ve Yardımcı paketler
  intl: ^0.19.0
  
  # Mevcut Flutter paketleri (zaten varsa tekrar eklemenize gerek yok)
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# Eğer eski versiyon kullanıyorsanız, bu versiyonları kullanabilirsiniz:
# Alternative versions for older projects:
# cloud_firestore: ^4.8.0
# firebase_auth: ^4.6.0  
# firebase_storage: ^11.2.0
# provider: ^6.0.5
# file_picker: ^5.3.0